---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: url-shortener-app
spec:
  template:
    spec:
      containers:
      - name: url-shortener
        image: <mention the docker image version with tag> # configure the ECR image endpoint
